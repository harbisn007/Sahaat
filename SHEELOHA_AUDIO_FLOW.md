# تدفق عمل أصوات الشيلوها - شرح تفصيلي

## البيانات الأساسية

```typescript
const CLAP_ASSET = require("@/assets/sounds/single-clap-short.mp3");
const CLAP_INTERVAL = 960;  // كل 960 ميلي ثانية
const LOOP_GAP = 150;        // فاصل بين الحلقات

const CROWD_FIXED = [
  { volume: 0.40, rate: 1.07 },  // صوت 1
  { volume: 0.30, rate: 1.06 },  // صوت 2
  { volume: 0.38, rate: 1.08 },  // صوت 3
  { volume: 0.38, rate: 1.05 },  // صوت 4
  { volume: 0.48, rate: 1.09 },  // صوت 5
];
```

---

## السيناريو الكامل

### المرحلة 0: المستخدم ينهي تسجيل الطاروق

```
1. المستخدم يضغط على زر الطاروق (يسجل صوته)
2. يرفع الصوت على S3
3. يحفظ في قاعدة البيانات
4. يرسل Socket.io للآخرين
```

### المرحلة 1: المستخدم يضغط على زر "خلوها" (Sheeloha)

```
play() يُستدعى مع:
{
  taroukUrl: "https://s3.../recording-123.m4a",
  taroukDuration: 3.5  // مدة الطاروق بالثواني
}
```

---

## التسلسل الزمني (Timeline)

### الثانية 0.0 - البداية الفورية

```
1. cleanup() - مسح أي تشغيل سابق
2. setAudioModeAsync() - إعداد الصوت
3. playClap() - تشغيل أول صفقة
   └─ ينشئ player للـ CLAP_ASSET
   └─ يشغله بـ volume = 0.25
   └─ يحرره بعد 1 ثانية

4. playFirstTwoOnly() - تشغيل الصوتين الأول والثاني فقط
   ├─ Crowd[0]: volume=0.40, rate=1.07
   │  └─ ينشئ player للطاروق
   │  └─ يشغله بـ volume=0.40
   │  └─ يشغله بـ speed=1.07x
   │  └─ يحرره بعد (3.5 + 2) = 5.5 ثانية
   │
   └─ Crowd[1]: volume=0.30, rate=1.06
      └─ ينشئ player للطاروق
      └─ يشغله بـ volume=0.30
      └─ يشغله بـ speed=1.06x
      └─ يحرره بعد 5.5 ثانية

5. startLoop() - بدء الحلقات
   └─ loopCount = 0
   └─ يجدول timeout بـ (3.5 * 1000) + 150 = 3650 ميلي ثانية
```

### الثانية 0.3 - الصفقات المتكررة

```
clapDelay = 300ms
بعد 300ms:
- تشغيل صفقة ثانية (clap)
- بدء clapInterval كل 960ms
  ├─ 0.3s: صفقة
  ├─ 1.26s: صفقة
  ├─ 2.22s: صفقة
  ├─ 3.18s: صفقة
  ├─ 4.14s: صفقة
  └─ ...
```

### الثانية 3.65 - الحلقة الثانية (loopCount = 1)

```
startLoop() يُستدعى مرة أخرى:
- loopCount = 1
- التحقق: if (loopCount > 1) ❌ لا (1 ليست > 1)
- لا تشغل الخمسة الآن
- جدول timeout جديد لـ 3650ms
```

### الثانية 7.3 - الحلقة الثالثة (loopCount = 2)

```
startLoop() يُستدعى:
- loopCount = 2
- التحقق: if (loopCount > 1) ✅ نعم!
- playCrowd() - تشغيل الخمسة أصوات كاملة
  ├─ Crowd[0]: volume=0.40, rate=1.07
  ├─ Crowd[1]: volume=0.30, rate=1.06
  ├─ Crowd[2]: volume=0.38, rate=1.08
  ├─ Crowd[3]: volume=0.38, rate=1.05
  └─ Crowd[4]: volume=0.48, rate=1.09
  
- كل واحد يحرر بعد 5.5 ثانية
- جدول timeout جديد لـ 3650ms
```

### الثانية 10.95 - الحلقة الرابعة (loopCount = 3)

```
playCrowd() تشتغل مرة أخرى (الخمسة)
- الحلقة تستمر إلى ما لا نهاية
```

---

## الصوت النهائي المسموع

### المرحلة الأولى (0 - 3.65 ثانية)

```
الصوتان الأول والثاني فقط يعملان:
- Crowd[0] (0.40 volume, 1.07x speed)
- Crowd[1] (0.30 volume, 1.06x speed)
- + صفقات متكررة (clap)

النتيجة: صوت حشد خفيف متزامن
```

### المرحلة الثانية (3.65 - 7.3 ثانية)

```
الصوتان الأول والثاني يستمران
+ الخمسة أصوات الجديدة تبدأ

النتيجة: صوت حشد أقوى (خمسة أصوات)
```

### المرحلة الثالثة (7.3+ ثانية)

```
الخمسة أصوات تعمل في حلقة مستمرة
كل 3.65 ثانية تبدأ حلقة جديدة

النتيجة: صوت حشد مستمر ومتزامن
```

---

## مثال بأرقام حقيقية

**لو الطاروق = 3 ثواني:**

```
الزمن (ثانية)  |  ما يحدث
─────────────────────────────────────────────
0.0           | البداية: صفقة + الصوتان 1,2
0.3           | صفقة متكررة تبدأ
0.96          | صفقة
1.92          | صفقة
2.88          | صفقة
3.0           | انتهاء الطاروق (الصوتان 1,2 ينتهيان)
3.15          | حلقة جديدة (loopCount=1) - لا تشغل الخمسة
3.65          | حلقة جديدة (loopCount=2) - تشغل الخمسة! ✅
4.61          | صفقة
5.57          | صفقة
6.53          | صفقة
7.3           | حلقة جديدة (loopCount=3) - الخمسة مرة أخرى
...           | استمرار الحلقات
```

---

## المميزات

✅ **التزامن الأفضل**: الصوتان الأول والثاني يبدآن فوراً بدون انتظار
✅ **تدرج صوتي**: يبدأ خفيف (صوتان) ثم يقوى (خمسة)
✅ **حلقات مستمرة**: الخمسة تعمل في حلقة دائمة بعد البداية
✅ **صفقات متزامنة**: تعمل طول الوقت بـ 960ms فاصل

---

## الملاحظات المهمة

1. **loopCount > 1** وليس **>= 1** لأن الحلقة الأولى (loopCount=0) تشغل الأول والثاني فقط
2. **LOOP_GAP = 150ms** يضيف فاصل صغير بين الحلقات لتجنب التداخل
3. **taroukDuration + 2** يضيف ثانيتين إضافيتين لضمان انتهاء الصوت كاملاً قبل الحرر
4. **setPlaybackRate()** تغير السرعة لتنويع الأصوات وتجنب الملل
